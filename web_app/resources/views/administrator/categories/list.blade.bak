@extends('layouts.app')
@section('content')<div class="card">
    <div class="card-header">Category</div>
    <div class="card-body table-responsive">
        <p><a href="{{ route('categories.add') }}" class="btn btn-info"><i class="fa-light fa-plus"></i> Add new</a></p>
        <table class="table table-bordered table-hover table-sm mb-0">
            <thead>
                <tr>
                    <th width="5%">#</th>
                    <th width="20%">Category</th>
                    <th width="30%">Slug name</th>
                    <th width="20%">Category Parent</th>
                    <th width="15%">Image</th>
                    <th width="5%">Update</th>
                    <th width="5%">Delete</th>
                </tr>
            </thead>
            <tbody>
                @foreach($categories as $value)
                <tr>
                    <td>{{ $loop->iteration }}</td>
                    <td>{{ $value->name }}</td>
                    <td>{{ $value->slug }}</td>
                    <td>{{ $value->parent_id }}</td>
                    <td>{!! $value->image ? '<img src="'.$value->image.'" alt="'.$value->name.'" style="max-width: 20px; max-height: 20px;">' : '' !!}</td>
                    <td class="text-center"><a href="{{ route('categories.update', ['id' => $value->id]) }}"><i class="fa-light fa-edit"></i></a></td>
                    <td class="text-center"><a href="{{ route('categories.delete', ['id' => $value->id]) }}" onclick="return confirm('Are you sure you want to delete the category {{ $value->name }}?')"><i class="fa-light fa-trash-alt text-danger"></i></a></td>
                </tr>
                @endforeach
            </tbody>
        </table>
    </div>
</div>
@endsection